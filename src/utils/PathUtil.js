//######################################################################################################################
// src/utils/PathUtil.js
//######################################################################################################################
(function() {

  var PATH_DELIMITER = /\[|\]?\./;

  Exact.PathUtil = {
    parse: function parse(path) {
      return path ? path.split(PATH_DELIMITER) : null;
    }
  };

})();
